services:
  - type: web
    plan: free
    name: render-joker
    env: python
    branch: render
    startCommand: pip install -r requirements.txt
    buildCommand: bash start
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: true
    autoDeploy: true
